// import _ from '/framework/belt';export function check(Store) {  return (name, str, timeout = 5000) => {    const { result, error } = Store.utils.validations[name](str);    // Check was successful    if (result) return true;    Store.alerts.add({      type: 'checks',      name,      text: error,    }, timeout);  };}
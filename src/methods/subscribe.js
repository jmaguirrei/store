export function subscribe(Store) {  return (registerNumber, observable) => {    console.log("registerNumber, observable", registerNumber, observable);    const findListener = Store.objects.listeners.find(item => item.observable === observable);    console.log("findListener", findListener);    const currentListeners = findListener.components;    console.log("currentListeners", currentListeners);    const isSubscribed = currentListeners.find(item => item.registerNumber === registerNumber);    console.log("isSubscribed", isSubscribed);    if (!isSubscribed) currentListeners.push({ registerNumber });  };}
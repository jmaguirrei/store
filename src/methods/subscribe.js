export function subscribe(Store) {  return (registerNumber, observable) => {    const observableIndex = Store.objects.listeners.findIndex(item => item.observable === observable);    const currentListeners = Store.objects.listeners[observableIndex].components;    const isSubscribed = currentListeners.find(item => item.registerNumber === registerNumber);    if (!isSubscribed) currentListeners.push({ registerNumber });  };}
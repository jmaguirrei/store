import { xhrRequest } from '../utils';export function getDataFromServer(args) {  const {    instance = null,    user_id,    lastTimestamp = 0,    localEntitiesIds = [],  } = args;  return new Promise((resolve, reject) => {    const requestArgs = {      user_id,      lastTimestamp,      localEntitiesIds,    };    xhrRequest('getModelData', requestArgs)    .then(res => {      if (res.error || !res.data) reject('Could not get data from server');      const { items, newTimestamp } = res.data;      resolve({ instance, items, newTimestamp });    });  });}